namespace AdapterPattern
{
    internal interface INewMath2
    {
        double ComputeAvg(double[] data);

        int FindMin(int[] data);

        double ComputeSphereVolume(double radius);
    }
}